name: Starter CI
on:
  push:
    branches: [ main ]
  workflow_dispatch:
  pull_request:
    branches:  [ main ]

    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Display runner details
        run: |
          echo "Workflow is running on ${{ runner.os }}"
          echo "more outputs from the CI"
